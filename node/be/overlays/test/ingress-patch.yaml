apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: webapp-ingress
spec:
  rules:
    - host: webapp.test
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: devops-playground-be
                port:
                  number: 88